# — Python cached & build files —
__pycache__/
*.py[cod]
*$py.class
htmlcov/

# — Virtualenv (if you’ve created it in the project root) —
bin/
include/
lib/
lib64/
pyvenv.cfg

# (If you ever name your venv folder “venv” instead, also add:)
venv/

# — pytest, coverage, mypy —
.pytest_cache/
.coverage
.mypy_cache/

# — Environment variables —
.env
.env.*

# — Docker —
# ignore any Docker build artifacts, override files, or secrets
docker-compose.override.yml

# — IDE/editor configs —
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# — macOS —
.DS_Store

# — logs —
*.log

# — other OS or editor temp files —
*.swp
*.tmp
*.bak
